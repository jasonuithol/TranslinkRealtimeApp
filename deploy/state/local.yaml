# Deployment state: the local "prod-ish" container (podman, port 8000,
# container `translink`, ghcr image, sharing the translink-data volume).
# Dev containers (tdev :8002, tpanel :8005) are rebuilt from the tree at
# will and are NOT tracked here. Same format and workflow as vps.yaml.

target: local
host: localhost:8000
updated: 2026-08-02

components:
  # Same pipeline as the vps image: commit → push → CI → GHCR → pull.
  # deploy.sh gates on git/CI state before pulling here too.
  image: { status: pending, reason: "planner: real-graph walk costs + latest-set-off tie-breaks" }
  # Basemaps live in the shared volume — built in place, nothing to ship.
  basemap-seq: { status: deployed, at: 2026-07-22 }
  basemap-mel: { status: deployed, at: 2026-07-26 }
  basemap-syd: { status: deployed, at: 2026-07-26 }
  basemap-ade: { status: deployed, at: 2026-07-26 }
  basemap-per: { status: deployed, at: 2026-07-26 }
  basemap-dar: { status: deployed, at: 2026-07-26 }
  basemap-qld: { status: deployed, at: 2026-07-27 }
  basemap-nsw: { status: deployed, at: 2026-07-28 }
  # G-NAF address DB — ingested straight into the shared volume.
  gnaf-db: { status: deployed, at: 2026-07-29 }
  # Walking street graph (planner walk-leg routing along roads).
  walkgraph-db: { status: deployed, at: 2026-07-30 }
  # OSM named-places index (business/POI search).
  places-db: { status: deployed, at: 2026-07-31 }
